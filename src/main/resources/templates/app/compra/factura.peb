<!DOCTYPE html>
<html lang="es">
{% include "fragments/head.peb" %}

<body class="d-flex flex-column min-vh-100">
{% include "fragments/navbar.peb" %}

<div class="container mt-5 pt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">Factura #{{ compra.id }}</h4>
                </div>
                <div class="card-body">
                    <div class="mb-4">
                        <h5>Datos de la compra:</h5>
                        <p><strong>Fecha:</strong> {{ compra.fechaCompra }}</p>
                        <p><strong>Comprador:</strong> {{ compra.propietario.nombre }} {{ compra.propietario.apellidos }}</p>
                        <p><strong>Email:</strong> {{ compra.propietario.email }}</p>
                    </div>
                    
                    <h5>Productos:</h5>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Producto</th>
                                    <th>Vendedor</th>
                                    <th class="text-end">Precio</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for producto in productos %}
                                <tr>
                                    <td>{{ producto.nombre }}</td>
                                    <td>{{ producto.propietario.email }}</td>
                                    <td class="text-end">{{ producto.precio }}€</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot>
                                <tr>
                                    <td colspan="2" class="text-end"><strong>Total:</strong></td>
                                    <td class="text-end"><strong>{{ total }}€</strong></td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    
                    <div class="mt-4 d-flex justify-content-between">
                        <a href="/app/miscompras" class="btn btn-secondary">Volver a mis compras</a>
                        <a href="/app/compra/factura/{{ compra.id }}/pdf" class="btn btn-danger">
                            <i class="bi bi-file-pdf"></i> Descargar PDF
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% include "fragments/footer.peb" %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
