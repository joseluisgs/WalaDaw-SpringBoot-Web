<!DOCTYPE html>
<html lang="es">
{% include "fragments/head" %}

<body class="d-flex flex-column min-vh-100">
{% include "fragments/navbar" %}

<div class="container mt-5 pt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="row">
                <div class="col-md-8">
                    <img src="{% if producto.imagen is empty %}https://placehold.it/1200x600?text=SIN%20IMAGEN{% else %}{{ producto.imagen }}{% endif %}" 
                         class="img-fluid rounded shadow" alt="{{ producto.nombre }}">
                </div>
                <div class="col-md-4">
                    <div class="text-center mb-4">
                        <img src="{% if producto.propietario.avatar is empty %}http://api.adorable.io/avatars/140/{{ producto.propietario.email }}.png{% else %}{{ producto.propietario.avatar }}{% endif %}" 
                             class="rounded-circle shadow" width="140" height="140" alt="Avatar">
                        <h5 class="mt-3">{{ producto.propietario.nombre }} {{ producto.propietario.apellidos }}<br>
                            <small class="text-muted">{{ producto.propietario.email }}</small>
                        </h5>
                    </div>
                    <div class="mb-3">
                        <h3>{{ producto.nombre }}</h3>
                    </div>
                    <div class="mb-3">
                        <h4 class="text-primary"><strong>{{ producto.precio }}€</strong></h4>
                    </div>
                    {% if authenticated and username != producto.propietario.email %}
                    <a href="/app/carrito/add/{{ producto.id }}" class="btn btn-lg btn-success w-100">
                        <i class="bi bi-cart-plus"></i> Comprar
                    </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>

{% include "fragments/footer" %}

<!-- Bootstrap 5.3 JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
